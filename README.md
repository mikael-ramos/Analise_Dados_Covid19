# Analise_Dados_Covid19

## Installation
* Install the packages `pip -r install requirements.txt`
* To run the application in this folder, run this command on your terminal `python -m streamlit run app.py`